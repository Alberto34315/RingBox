<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ringbox">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_boxeo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_boxeo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RingBox">
        <activity
            android:name=".Views.FormActivity"
            android:label="@string/title_activity_form"
            android:theme="@style/Theme.RingBox.NoActionBar"></activity>
        <activity
            android:name=".Views.ListActivity"
            android:label="@string/title_activity_list"
            android:theme="@style/Theme.RingBox.NoActionBar" />
        <activity
            android:name=".Views.LogoActivity"
            android:theme="@style/Theme.RingBox.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>